#include <bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin >> n; long sum=0;

    if (n>0 && n<=10) {
    int arr[n];
   
    for (int i=0;i<n;i++){
        cin >>arr[i];
        sum+=arr[i];
    }
    }
    cout << sum;
}
